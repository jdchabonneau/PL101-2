#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../pegjs/bin/pegjs" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../pegjs/bin/pegjs" "$@"
  ret=$?
fi
exit $ret
